<iframe
    src="https://open.spotify.com/embed/{{ $type }}/{{ $id }}"
    width="{{ $width ?? 300 }}"
    height="{{ $height ?? 380 }}"
    frameborder="0"
    allowtransparency="true"
    allow="encrypted-media"
></iframe>